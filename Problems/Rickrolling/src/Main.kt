import java.util.*

fun main() {
    val scanner = Scanner(System.`in`)
    val a = scanner.next() + " "
    val b = scanner.next() + " "
    val c = scanner.next() + " "
    val d = scanner.next() + " "
    val e = scanner.next() + " "

    print(a + b + c + d + e)
}